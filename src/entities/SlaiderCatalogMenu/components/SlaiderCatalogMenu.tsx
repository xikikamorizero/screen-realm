import React from "react";
import styled from "styled-components";

export const SlaiderCatalogMenu = () => {
  return (
    <Container>
 
    </Container>
  );
};

const Container = styled.div`
  width: 100%;
  display: flex;
  gap:20px;
`;