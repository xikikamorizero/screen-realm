import styled from "styled-components";

export const ItemCatalogMenu = styled.div`
  padding: 15px;
  color: var(--creator);
  text-align: center;
  font-size: 16px;
  font-style: normal;
  font-weight: 500;
  line-height: normal;
  
  :hover {
    color: var(--white);
  }

  cursor: pointer;

  -moz-user-select: none;
  -khtml-user-select: none;
  user-select: none;
`;
